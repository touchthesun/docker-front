language: generic
sudo: required
services:
  - docker


before_install:
  - docker build -t touchthesun/docker-front -f Dockerfile.dev .


  script:
  - docker run -e CI=true touchthesun/docker-front npm run test -- --coverage  
